---
- name: Customer Hijack Execution
  # Use FQCN so the vendor module resolves from the embedded collection
  osac.demo.fancy_echo_v1:
    msg: "{{ driver_msg }}"
  register: output

- name: Show driver output
  debug:
    msg: "{{ output.result_msg }}"
